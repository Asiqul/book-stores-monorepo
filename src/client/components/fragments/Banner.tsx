import SwiperBanner from '@components/elements/body/sliders/Swiper';
import Wrapper from '@components/elements/body/sliders/Wrapper';

const Banner = () => {
    return (
        <Wrapper>
            <SwiperBanner />
        </Wrapper>
    );
};

export default Banner;
